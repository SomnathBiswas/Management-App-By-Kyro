To run the backend 

1. cd backend
2. python -m venv venv
3. venv\Scripts\activate
4. Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process -Force
5. uvicorn server:app --reload --host 0.0.0.0 --port 8000

To run the frontend

1. cd frontend
2. yarn start
